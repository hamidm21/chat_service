const express = require('express');
const router = express.Router();
const chat_controller = require('../lib/controllers/chat_controller');

/* GET users listing. */

//router.get('/', chat_controller.home);

//router.get('/join' , chat_controller.join);

module.exports = router;
